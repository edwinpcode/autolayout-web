version: "3.8"
services:
  ifinforce:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3002:8080"
    network_mode: bridge
    extra_hosts:
      - "host.docker.internal:host-gateway"
